# name: Push Action

# on:
#   pull_request:
#     types: [ opened, synchronize, reopened, ready_for_review ]
#     branches:
#       - release_note_test

# jobs:
#   generate-and-upload-release-note:
#     runs-on: ubuntu-latest
#     outputs:
#       release-note: ${{ steps.generate-release-note.outputs.release-note }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#       - name: set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: 3.11
#       - name: Install requirements
#         run: pip install releaser

#       - name: add tag
#         env:
#           GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
#           BRANCH: ${{github.event.pull_request.head.ref}}
#           REPO: ${{github.repository}}
#           COMMIT: ${{github.event.pull_request.head.sha}}
#         run: |
#           chmod +x ./.github/workflows/add-tag.sh
#           ./.github/workflows/add-tag.sh  

#       - name: Generate release note
#         id: generate-release-note
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           BRANCH: ${{github.event.pull_request.head.ref}}
#         run: python .github/workflows/create_release_note.py --description 'release description' ${{ github.ref }}
#       - name: Upload release note
#         uses: actions/upload-artifact@v2
#         with:
#           name: release-note
#           path: ${{ steps.generate-release-note.outputs.release-note }}

name: Create release tag and release note.

on:
  pull_request:
    types: [ opened, synchronize, reopened, ready_for_review ]
    branches:
      - release_note_test

jobs:
  create-release-tag:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      # タグを生成する　release_tagにブランチ名を設定
      - name: Generate release tag
        id: release_tag
        run: |
          branch_name=$(echo ${{ github.event.pull_request.head.ref }} | sed 's/^release\///')
          echo $branch_name
        shell: bash

      # リリースノートの本文を生成する
      - name: Generate release note
        id: release_note
        run: |
          release_note=$(python .github/workflows/generate_release_note.py)
          echo $release_note
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: ${{ github.event.pull_request.head.ref }}
        shell: bash

      - name: Output variables
        run: |
          release_note="${{ steps.release_note.outputs.stdout }}"
          echo $'release_note = \n'"$release_note"
          echo "release_tag = ${{ steps.release_tag.outputs.stdout }}"
          echo "github.repository = ${{  github.repository }}"
        shell: bash

      # タグを切り、リリースノートを作成する
      - name: Create Release
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -d "{ \"tag_name\": \"${{ steps.release_tag.outputs.stdout }}\", \"name\": \"${{ steps.release_tag.outputs.stdout }}\", \"body\": \"${{ steps.release_note.outputs.stdout }}\"}" \
            https://api.github.com/repos/${{ github.repository }}/releases
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash